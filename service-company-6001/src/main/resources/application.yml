#server:
#  port: 6001
#  tomcat:
#    uri-encoding: utf-8
#    max-swallow-size: -1
spring:
  application:
    name: company-service
#  profiles:
#    active: dev
mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  global-config:
    db-config:
      id-type: assign_id
    banner: false